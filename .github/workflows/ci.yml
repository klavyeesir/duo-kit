name: CI
on: [push, pull_request]

jobs:
  lint-tests:
    runs-on: ubuntu-latest
    container: swift:latest
    steps:
      - uses: actions/checkout@v4
      - name: Build
        run: swift build
      - name: Fixture tests
        run: bash scripts/test-fixtures.sh
